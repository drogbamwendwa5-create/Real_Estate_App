class ImageSimilarity{async compareImages(images1,images2){if(!images1||!images2)return 0;const urls1=new Set(images1.map(i=>this.normalizeUrl(i.url||i)));const urls2=new Set(images2.map(i=>this.normalizeUrl(i.url||i)));const intersection=[...urls1].filter(u=>urls2.has(u));return Math.round((intersection.length/Math.max(urls1.size,urls2.size))*100);}normalizeUrl(url){try{return url.split("?")[0].replace(/^https?:\/\/,"").replace(/^www\./,"").toLowerCase();}catch(e){return url;}}async checkImageHash(url){return null;}}module.exports=ImageSimilarity;