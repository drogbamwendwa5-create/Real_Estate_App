const v=require("../config/validation.config");class LocationValidator{validate(lat,lng,county){const e=[];if(lat&&lng){const la=parseFloat(lat);const ln=parseFloat(lng);if(isNaN(la)||isNaN(ln))e.push({field:"coordinates",message:"Invalid coordinates",code:"INVALID_COORDS"});if(la<v.coordinates.minLatitude||la>v.coordinates.maxLatitude)e.push({field:"latitude",message:"Out of Kenya bounds",code:"LAT_OUT_OF_BOUNDS"});if(ln<v.coordinates.minLongitude||ln>v.coordinates.maxLongitude)e.push({field:"longitude",message:"Out of Kenya bounds",code:"LNG_OUT_OF_BOUNDS"});}return{isValid:e.length===0,errors:e};}}module.exports=LocationValidator;