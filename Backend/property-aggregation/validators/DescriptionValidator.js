const v=require("../config/validation.config");class DescriptionValidator{validate(d){const w=[];if(!d){w.push({field:"description",message:"Missing description",code:"MISSING_DESC"});return{isValid:true,warnings:w};}if(d.length<v.description.minLength)w.push({field:"description",message:"Too short",code:"SHORT_DESC"});if(d.length>v.description.maxLength)w.push({field:"description",message:"Too long",code:"LONG_DESC"});if(v.description.checkPhoneNumbers){const p=d.match(/\b\d{10,13}\b/g);if(p&&p.length>2)w.push({field:"description",message:"Spam phones",code:"SPAM_PHONES"});}return{isValid:true,warnings:w};}}module.exports=DescriptionValidator;