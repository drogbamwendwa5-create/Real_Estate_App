const v=require("../config/validation.config");class ImageValidator{validate(images){const e=[];const w=[];if(!images||!Array.isArray(images)||images.length===0){e.push({field:"images",message:"No images",code:"MISSING_IMAGES"});return{isValid:false,errors:e,warnings:w};}if(images.length<v.images.minImages)e.push({field:"images",message:"Insufficient images",code:"INSUFFICIENT_IMAGES"});if(images.length>v.images.maxImages)w.push({field:"images",message:"Too many images",code:"TOO_MANY_IMAGES"});images.forEach((img,i)=>{const u=typeof img==="string"?img:img.url;if(!u){e.push({field:"images["+i+"]",message:"Missing URL",code:"MISSING_IMAGE_URL"});return;}const f=this.getFmt(u);if(!v.images.acceptedFormats.includes(f))e.push({field:"images["+i+"]",message:"Invalid format: "+f,code:"INVALID_FORMAT"});});return{isValid:e.length===0,errors:e,warnings:w};}getFmt(u){const m=u.match(/\.(jpg|jpeg|png|webp|gif|bmp)(\?|$)/i);return m?m[1].toLowerCase():"unknown";}async checkQuality(u){try{const a=require("axios");const r=await a.head(u,{timeout:10000});const ct=r.headers["content-type"]||"";if(!ct.startsWith("image/"))return{isValid:false,reason:"Not image"};const cl=parseInt(r.headers["content-length"]||0);if(cl>0&&cl<10240)return{isValid:false,reason:"Too small"};return{isValid:true};}catch(e){return{isValid:false,reason:e.message};}}}module.exports=ImageValidator;