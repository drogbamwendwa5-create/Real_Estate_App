const v=require("../config/validation.config");const p=require("../config/property.config");class PriceValidator{validate(price,lt,c){const e=[];const n=parseFloat(price);if(isNaN(n)||n<=0){e.push({field:"price",message:"Invalid price",code:"INVALID_PRICE"});return{isValid:false,errors:e};}const r=p.priceRanges[lt||"for-sale"];if(r&&(n<r.min||n>r.max))e.push({field:"price",message:"Price out of range",code:"PRICE_OUT_OF_RANGE"});if(n<v.price.minPrice)e.push({field:"price",message:"Price too low",code:"PRICE_TOO_LOW"});if(n>v.price.maxPrice)e.push({field:"price",message:"Price too high",code:"PRICE_TOO_HIGH"});return{isValid:e.length===0,errors:e};}}module.exports=PriceValidator;