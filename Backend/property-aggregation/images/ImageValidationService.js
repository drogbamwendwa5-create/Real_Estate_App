const v=require("../config/validation.config");const axios=require("axios");class ImageValidationService{constructor(){this.fmts=v.images.acceptedFormats;}async validateImages(imgs){const r=[];for(const i of imgs){const u=typeof i==="string"?i:i.url;r.push({url:u,...await this.validateSingle(u)});}return r;}async validateSingle(u){if(!u)return{isValid:false,reason:"Missing URL"};const f=this.getFmt(u);if(!this.fmts.includes(f))return{isValid:false,reason:"Invalid format"};try{const r=await axios.head(u,{timeout:10000});const ct=r.headers["content-type"]||"";if(!ct.startsWith("image/"))return{isValid:false,reason:"Not image"};const cl=parseInt(r.headers["content-length"]||0);if(cl>0&&cl<10240)return{isValid:false,reason:"Too small"};return{isValid:true,format:f};}catch(e){return{isValid:false,reason:"Broken URL"};}}getFmt(u){const m=u.match(/\.(jpg|jpeg|png|webp)(\?|$)/i);return m?m[1].toLowerCase():"unknown";}async checkDuplicate(imgs){const seen=new Map();const dups=[];for(const i of imgs){const u=(typeof i==="string"?i:i.url).split("?")[0];if(seen.has(u))dups.push(u);else seen.set(u,true);}return{duplicates:dups,count:dups.length};}}module.exports=ImageValidationService;